import { FunctionalComponent, h } from "preact";
import { Map } from "src/components/map";

const RouteHome: FunctionalComponent = () => {
  return <Map filename="home" />;
};

export default RouteHome;
